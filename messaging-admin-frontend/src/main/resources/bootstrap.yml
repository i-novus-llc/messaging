spring:
  application:
    name: ${project.artifactId}
  cloud:
    consul:
      host: consul-agent.local
      port: 8500
      config:
        enabled: ${config.server.enable}
        fail-fast: false
