spring:
  application:
    name: messaging-admin-frontend
  cloud:
    consul:
      host: consul-agent.local
      port: 8500
      config:
        enabled: ${config.server.enable:false}
        fail-fast: false
