{
  "modules": {
    "Messaging": {
      "src": "novus/messaging/messaging",
      "notWaitReady": true,
      "options" : {
        "address" : {"ws"   : "${messaging.system.ws.url}",
                     "rest" : "${messaging.system.rest.url}"},
        "enabled" : ${messaging.system.notifications.enabled}
      }
    }
  },
  "formattingFunctions": {
    "custom": "novus/messaging/formatting.date"
  }
}
